---
########################################################################################
# 
# Required configuration for the associated "AddCliArgsToBuildContext" custom groovy task. 
#
# This YAML file defines the AddCliArgsToBuildContext task 
#
# The groovy script "CLIToBuildContext.groovy" will be 
# automatically discovered as long as it is placed in the $DBB_BUILD/groovy directory.
#
#
# Setup Instructions: 
# - Copy this configuration YAML file and the associated groovy script to your $DBB_BUILD
#   directory. Important: Ensure the Groovy script is in the groovy sub-directory so it 
#   will be discovered automatically. 
# - Include this YAML file in dbb-build.yaml to source the task configuration. 
# - Add the AddCliArgsToBuildContext task to your build lifecycle in dbb-build.yaml ordered 
#   configuration before you want to make use of the variables. 
#
########################################################################################

version: 1.0.0
tasks: 
  - task: CLIToBuildContext